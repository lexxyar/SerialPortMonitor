# Serial Port Monitor

Simple Serial Port monitor via NodeJS.

> Call `listOfPorts()` procedure to define ports with connections.

> Use `connect()` function to get data from Serial Port. In code used COM4 port with 115200 baud rate.
